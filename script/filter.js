export class Filter {
	constructor() {
		// const $products = document.querySelectorAll('.main__products-product')
		// const $checkboxes = document.querySelectorAll('.filter__group-list-item input')
		// const $submitButton = document.querySelector('.filter__button')
		// const $typeCheckbox = document.querySelectorAll('#type input')

		// const arr = [1, 2, 3, 4, 5, 6]

		// console.log($typeCheckbox.indexOf('braslets'));

		// $submitButton.addEventListener('click', () => {
		// 	$products.forEach(item => { item.style.display = 'none' })
		// 	$products.forEach(item => {
		// 		if ($typeCheckbox.indexOf(el => { item.getAttribute('data-type') && el.checked })) {
		// 			item.style.display = 'flex'
		// 		}
		// 	})
		// })

		// function findFilter(filter) {
		// 	return
		// }


	}
}